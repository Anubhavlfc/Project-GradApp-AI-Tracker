# GradTrack AI - Environment Variables

# OpenAI API Key (required)
OPENAI_API_KEY=your_openai_api_key_here

# Database path (optional, defaults to gradtrack.db)
DATABASE_PATH=gradtrack.db

# ChromaDB path (optional, defaults to ./chroma_data)
CHROMA_PATH=./chroma_data

# Server settings (optional)
HOST=0.0.0.0
PORT=8000
