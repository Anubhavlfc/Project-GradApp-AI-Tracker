# Environment variables for GradTrack AI
# Copy this file to .env and fill in your values

# OpenAI API Key (required for AI functionality)
OPENAI_API_KEY=your_openai_api_key_here

# Optional: Model to use (defaults to gpt-4o)
# OPENAI_MODEL=gpt-4o

# Optional: Database path (defaults to ./gradtrack.db)
# DATABASE_PATH=./gradtrack.db

# Optional: Memory storage path (defaults to ./memory_store)
# MEMORY_PATH=./memory_store
